class Product {
  int id, qty, stockQty;
  String key, name, imageUrl, price, desc;
  Product(
      {this.name,
      this.imageUrl,
      this.desc,
      this.key,
      this.price,
      this.qty,
      this.stockQty});
}
